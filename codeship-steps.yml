- name: ci
  service: maven
  command: mvn package
- service: publish
  type: push
  image_name: keyholesoftware/khs-project-service
  image_tag: "{{.CommitID}}"
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
- service: deploy
  command: bash deploy.sh
